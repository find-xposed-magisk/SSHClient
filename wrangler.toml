name = "ssh-web-client"

[build]
command = "npm install && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "18"

[site]
bucket = "./dist"

# Workers 配置
[workers_dev]
enabled = true

[[workers]]
name = "ssh-worker"
main = "src/worker.js"