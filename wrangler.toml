name = "ssh-web-client"
compatibility_date = "2023-12-01"

[build]
command = "npm install && npm run build"
dist = "dist"

[site]
bucket = "dist"

# Workers 配置
[workers_dev]
enabled = true

[[workers]]
name = "ssh-worker"
main = "src/worker.js"